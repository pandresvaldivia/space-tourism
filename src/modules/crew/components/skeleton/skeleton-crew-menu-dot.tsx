import { component$ } from "@builder.io/qwik";

export const SkeletonCrewMenuDot = component$(() => {
  return (
    <div class="block h-2.5 lg:h-3.5 bg-white aspect-square rounded-full opacity-50" />
  );
});
